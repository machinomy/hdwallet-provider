{
  "name": "@machinomy/hdwallet-provider",
  "version": "1.3.0",
  "description": "HD Wallet-enabled Web3 provider",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "Apache-2.0",
  "scripts": {
    "build": "yarn clean && tsc --project tsconfig.json",
    "clean": "rm -rf dist/",
    "lint": "tslint --format stylish --project . --exclude './node_modules/**/*.ts'",
    "prepublish": "yarn build --outDir dist/",
    "test": "yarn build && mocha src/**/*.test.js"
  },
  "files": [
    "dist/"
  ],
  "dependencies": {
    "bip39": "^2.5.0",
    "eth-hd-keyring": "^1.2.2",
    "ethereumjs-tx": "^1.3.4",
    "web3-provider-engine": "^14.0.5"
  },
  "devDependencies": {
    "@machinomy/types-eth-hd-keyring": "^0.0.1",
    "@machinomy/types-ethereumjs-tx": "^0.1.0",
    "@machinomy/types-ethereumjs-wallet": "^0.0.12",
    "@machinomy/types-web3": "^0.0.7",
    "@machinomy/types-web3-provider-engine": "^0.0.5",
    "@types/bip39": "^2.4.0",
    "@types/mocha": "^5.2.0",
    "ganache-core": "^2.1.0",
    "mocha": "^5.1.1",
    "tslint": "^5.10.0",
    "tslint-config-standard": "^7.0.0",
    "typescript": "^2.8.3",
    "web3": "^0.20"
  }
}
